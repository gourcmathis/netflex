version: '3.3'

services:

  frontend:
    container_name: frontend
    build: ./nx-vuejs/nx-frontend
    volumes:
      - './nx-vuejs/nx-frontend:/app'
      - '/app/node_modules'
    
    ports:
      - 8080:8080